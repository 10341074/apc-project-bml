#ifndef COLOR_H
#define COLOR_H

#define SEPARATOR ','
using size_type = const std::size_t;

enum Color {White, Blue, Red};
using MatrixStruct = std::vector<std::vector<Color>>;

// using Scalar = Color;
#endif
